import styled from "styled-components"

export const Container = styled.div`
  max-width: 100vw;
  overflow: hidden;
  padding: 0 10px;

  h2 {
    color: #ffffff;
    font-size: 24px;
    margin: 50px 0 10px;
  }

  .swiper-wrapper {
    display: flex;
    max-width: 100vw;
  }
`
